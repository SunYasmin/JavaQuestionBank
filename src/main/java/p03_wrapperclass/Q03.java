package p03_wrapperclass;

public class Q03 {

    /*
    What are the differences between valueOf(String s)  and  parseByte(String s) methods?

    parseByte() returns primitive integer type (int) , whereas valueOf() returns java.lang.Integer,
    which is the object representative of the integer.
    There are circumstances where you might want an Integer object, instead of a primitive type.

    Of course , another obvious difference is that valueOf() is an instance method whereas parseByte() is a static method.

    parseByte(),(statik method) ilkel tamsayı türünü (int) döndürürken, valueOf(),(instance method) Java.lang.Integer'ı döndürür,
     tamsayının nesne temsilcisidir.
     İlkel tür yerine Tamsayı nesnesi isteyebileceğiniz durumlar vardır.

     Elbette bir başka bariz fark da valueOf()'un bir örnek instance yöntem olması, parseByte()'nin ise statik bir yöntem olmasıdır.


     */





}
